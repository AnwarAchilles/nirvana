<?php


$config['request'] = [
  
  // Request mode (e.g., 'development' or 'production')
  'mode'=> 'development',

  // URL for the API endpoint
  'url' => '',
  
  // Token for authentication (if required)
  'token'=> '',
  
  // Request headers (e.g., 'Content-Type')
  'headers' => [

    'Content-Type: application/json',
    
  ],

];